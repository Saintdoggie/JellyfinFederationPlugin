{
  "guid": "495feadb-d27f-46c3-bb9b-0732ae8926fa",
  "name": "Jellyfin Federation",
  "description": "Aggregate content from multiple Jellyfin servers into unified virtual libraries",
  "overview": "The Jellyfin Federation Plugin allows you to create a unified media library by aggregating content from multiple remote Jellyfin servers. Stream content from any server without file synchronization.",
  "owner": "Angablade",
  "category": "General",
  "version": "0.0.55",
  "changelog": "Fixed the client-side badge script (Web/federation-badge.js): a stray pair of literal NUL bytes had crept into the origin-filter code, which is harmless to the browser but corrupts anything expecting normal text - fixed by using a proper \\u0000 escape instead. Also fixed a real display bug: the Download/Hide pills' progress text was being written into the wrong inner element (their icon, not their label), which is why the download pill visibly garbled itself mid-download instead of showing a percentage cleanly.\n\nMoved \"Download to server\" out of a standalone pill next to the title - it was fighting a stylized show-logo title for space and looked broken - into jellyfin-web's own \"more actions\" (⋮) menu, alongside Edit metadata/Refresh metadata/etc., with progress now shown as a small toast instead. It's cloned from a real menu item already in that menu (icon and label swapped) rather than styled from scratch, so it automatically matches whatever theme is active.\n\nMade the floating origin-filter control and the download-progress toast theme-adaptive: they now pull their colors from the active theme's own CSS variables (falling back to the previous dark palette if a theme doesn't define them), so a custom theme's colors carry through instead of a fixed dark chip regardless of theme. The corner badge and title/hide pills stay a fixed solid color on purpose - they sit over poster art and backdrop images, not page background, the same reasoning behind Jellyfin's own always-dark played-checkmark badge.\n\nAlso fixed the badge/download/hide scripts never actually running on this install: jellyfin-web's index.html was owned by root while the server process runs as a regular user, so the plugin's every attempt to inject its client script into the page failed silently on every startup - permission fixed, script now injects and loads correctly.\n\n194 tests (unchanged - this release is client-script and deployment fixes, no server-side logic changed).",
  "targetAbi": "10.11.0.0",
  "timestamp": "2026-08-18T00:00:00Z",
  "imageUrl": "thumb.png"
}
